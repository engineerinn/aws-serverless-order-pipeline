[pytest]
testpaths = lambdas

# Every lambda folder has a test_lambda_function.py. importlib mode derives a
# unique module name from the full path, so the duplicate basenames don't
# collide the way they would under the default prepend mode.
addopts = --import-mode=importlib -q
